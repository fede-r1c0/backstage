name: CI/CD Pipeline Completo

on:
  push:
    branches: [main, develop]
    paths:
      - 'packages/**'
      - 'package.json'
      - 'yarn.lock'
      - 'app-config*.yaml'
      - 'plugins/**'
      - 'examples/**'
      - 'catalog-info.yaml'
      - 'backstage.json'
  pull_request:
    branches: [main]
    paths:
      - 'packages/**'
      - 'package.json'
      - 'yarn.lock'
      - 'app-config*.yaml'
      - 'plugins/**'
      - 'examples/**'
      - 'catalog-info.yaml'
      - 'backstage.json'
  release:
    types: [created]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deploy environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - development
          - staging
          - production

env:
  NODE_VERSION: '20.19.4'
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/backstage

jobs:
  # Job 1: Análisis y Validación
  validate:
    name: 🔍 Validate & Lint
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          # Asegurar que se copien TODOS los archivos
          fetch-depth: 0
          # Verificar que yarn.lock esté presente
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Verify repository structure
        run: |
          echo "🔍 Verificando estructura del repositorio..."
          echo "📁 Contenido del directorio:"
          ls -la
          echo ""
          echo "📄 yarn.lock existe:"
          if [ -f "yarn.lock" ]; then
            echo "✅ yarn.lock encontrado"
            echo "📏 Tamaño: $(wc -l < yarn.lock) líneas"
            echo "🔑 Hash: $(sha256sum yarn.lock | cut -d' ' -f1)"
          else
            echo "❌ yarn.lock NO encontrado"
            echo "📁 Contenido de .yarn/:"
            ls -la .yarn/ || echo "No .yarn/ directory"
            echo ""
            echo "📄 .yarnrc.yml contenido:"
            cat .yarnrc.yml || echo "No .yarnrc.yml"
            echo ""
            echo "📦 package.json scripts disponibles:"
            cat package.json | grep -A 20 '"scripts"' || echo "No scripts found"
          fi

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Get yarn cache directory
        id: yarn-cache-dir
        run: echo "dir=$(yarn config get cacheFolder)" >> $GITHUB_OUTPUT

      - name: Cache yarn dependencies
        uses: actions/cache@v4
        with:
          path: |
            ${{ steps.yarn-cache-dir.outputs.dir }}
            .yarn/cache
            .yarn/install-state.gz
            node_modules
          # Cache key que funciona con o sin yarn.lock
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') || 'no-lock' }}
          restore-keys: |
            ${{ runner.os }}-yarn-

      - name: Install dependencies
        run: |
          echo "📦 Instalando dependencias..."
          echo "🔒 yarn.lock debe estar presente en el repo"

          # Verificar que yarn.lock esté presente
          if [ ! -f "yarn.lock" ]; then
            echo "❌ ERROR: yarn.lock no encontrado"
            echo "💡 Solución: Commit yarn.lock al repositorio"
            echo "📋 Comandos:"
            echo "   yarn install  # Generar lockfile"
            echo "   git add yarn.lock"
            echo "   git commit -m 'add yarn.lock'"
            echo "   git push origin main"
            exit 1
          fi

          # Instalar con lockfile existente
          yarn install --immutable
          echo "✅ Instalación exitosa con --immutable"

      - name: Run linters
        run: |
          echo "🔍 Running linters..."
          yarn lint:all 2>/dev/null || echo "⚠️ Linter warnings found"

      - name: Type check
        run: |
          echo "📝 Type checking..."
          yarn tsc

      - name: Check formatting
        run: |
          echo "🎨 Checking code format..."
          yarn prettier:check 2>/dev/null || echo "⚠️ Format issues found"

      - name: Security audit
        run: |
          echo "🔐 Security audit..."
          echo "🔍 Usando yarn npm audit (Yarn 4 compatible)..."

          # Yarn 4 usa 'yarn npm audit' en lugar de 'yarn audit'
          # npm directo no funciona porque no hay package-lock.json
          # Usar comando más eficiente: solo vulnerabilidades moderadas o más críticas
          yarn npm audit --severity moderate --recursive || echo "⚠️ Some vulnerabilities found"

  # Job 2: Build Backstage
  build-backend:
    name: 🏗️ Build Backstage
    runs-on: ubuntu-24.04
    needs: validate
    permissions:
      contents: read
      packages: write
    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
      image-digest: ${{ steps.build.outputs.digest }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: network=host

      - name: Log in to Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value={{date 'YYYYMMDD'}}-{{sha}},enable={{is_default_branch}}

      - name: Build multi-arch image
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: packages/backend/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=backstage-main
          cache-to: type=gha,scope=backstage-main,mode=max
          provenance: false
          sbom: false

      - name: Test image locally
        if: github.event_name == 'pull_request'
        run: |
          echo "🧪 Testing Docker image..."
          # Load image for testing (single arch for speed)
          docker buildx build --load \
            --platform linux/amd64 \
            -f packages/backend/Dockerfile \
            -t test-image:${{ github.sha }} .

          # Run container
          docker run -d --name test-backstage \
            -p 7007:7007 \
            test-image:${{ github.sha }}

          # Wait for startup
          sleep 10

          # Test health
          curl -f http://localhost:7007/api/catalog/entities || \
            (docker logs test-backstage && exit 1)

          # Cleanup
          docker stop test-backstage && docker rm test-backstage
          echo "✅ Image test passed"

    # Job 3: Análisis de Seguridad
  security-scan:
    name: 🔐 Security Scan
    runs-on: ubuntu-24.04
    needs: build-backend
    if: github.event_name != 'pull_request'
    continue-on-error: true
    permissions:
      contents: read
      security-events: write
      actions: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # Solo necesitamos el código para el upload SARIF
          fetch-depth: 1

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          # Usar tag 'latest' que sabemos que existe
          image-ref: ${{ env.REGISTRY }}/${{ github.repository_owner }}/backstage:latest
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'
          # Solo vulnerabilidades, no secrets (más rápido)
          scanners: 'vuln'

      - name: Upload Trivy results to GitHub Security
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'

      - name: Create security summary
        if: always()
        run: |
          echo "## 🔐 Security Scan Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Image scanned for CVEs using Trivy" >> $GITHUB_STEP_SUMMARY
          echo "Results uploaded to Security tab" >> $GITHUB_STEP_SUMMARY

  # Job Final: Summary
  pipeline-summary:
    name: 📊 Pipeline Summary
    runs-on: ubuntu-24.04
    needs: [validate, build-backend, security-scan]
    if: always()

    steps:
      - name: Generate Complete Summary
        run: |
          echo "# 📊 CI/CD Pipeline Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Pipeline Status" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Stage | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🔍 Validation | ${{ needs.validate.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🏗️ Build | ${{ needs.build-backend.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🔐 Security | ${{ needs.security-scan.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [[ "${{ github.event_name }}" != "pull_request" ]]; then
            echo "## 🐳 Container Registry" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### Pull Commands" >> $GITHUB_STEP_SUMMARY
            echo '```bash' >> $GITHUB_STEP_SUMMARY
            echo "# Latest" >> $GITHUB_STEP_SUMMARY
            echo "docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "# Specific SHA" >> $GITHUB_STEP_SUMMARY
            echo "docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "**Build Time**: $(date -u +"%Y-%m-%d %H:%M:%S UTC")" >> $GITHUB_STEP_SUMMARY
          echo "**Triggered by**: @${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
